<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"
      xml:lang="zh-CN"
      xml:base="{{ handler.settings['forum_url'] }}/">
    <title>{{ handler.settings['forum_title'] }}-{{ name }}</title>
    <id>{{ handler.settings['forum_url'] }}{{ url }}/feed</id>
    <link href="{{ handler.settings['forum_url'] }}{{ url }}" rel="alternate" type="text/html"/>
    <link href="{{ handler.settings['forum_url'] }}{{ url }}/feed" rel="self"/>
    <link rel="hub" href="http://pubsubhubbub.appspot.com/"/>
    <updated>{{ time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime(posts[1]['posttime']+28800)) }}</updated>
    <author>
        <name>whtsky</name>
    </author>
    {% for post in posts %}
    <entry>
        <link href="{{ handler.settings['forum_url'] }}topics/{{ post['_id'] }}"/>
        <id>{{ handler.settings['forum_url'] }}topics/{{ post['_id'] }}</id>
        <title>{{ post['title'] }}</title>
        <content type="html">
            {{ post['content'] }}
        </content>
        <author>
            <name>{{ post['author'] }}</name>
        </author>
        <updated>{{ time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime(post['posttime']+28800)) }}</updated>
    </entry>
    {% end %}

</feed>